# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Claude Code local session state (permissions etc.) — not shared config
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Loose reference/design material at the repo root — not imported by src/ or
# public/ (those have their own copies of anything actually used). Kept on
# disk, just not tracked, to avoid a ~120MB first commit. Untrack this block
# if any of these should actually ship with the repo.
/*.zip
/Paradela*.png
/hf_*.png
/WhatsApp Image*.jpeg
/original-*.mp4
/pixellab-*.gif
/robot.png
/robot-*.png
/rhytmo_final/
